/* global require */
/**
 * Test if the Campaign items tab is pre selected when we go to the Campaign maintenance page
 */
var config = require('../config');
require('../setup');
require('../steps/goToCampaignMaintenancePage');